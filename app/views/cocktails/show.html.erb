<div class="container">
  <h1><%= @cocktail.name %></h1>
  <ul>
    <% @doses.each do |dose| %>
      <li><%= dose.ingredient.name %> — <%= dose.description %>
          <%= link_to "Delete", dose_path(dose), method: :delete %>
      </li>
    <% end %>
  </ul>

  <%= link_to "Add a Dose", new_cocktail_dose_path(@cocktail) %>
  <p><%= link_to "Back to Cocktails", cocktails_path %></p>
</div>
